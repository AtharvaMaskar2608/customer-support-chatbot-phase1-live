## 1. Package scaffold & dependencies

- [x] 1.1 Create `pyproject.toml` declaring ALL backend deps up front — runtime: `fastapi`, `uvicorn[standard]`, `pydantic` (v2), `httpx`, `anthropic`, `openai`, `psycopg[binary]`, `pgvector`; dev/eval extras group: `deepeval`, `pytest`, `pytest-asyncio`, `respx` (the single repo-wide httpx mock). Generate the lockfile. Done: `pip install -e ".[dev]"` (or `uv sync`) succeeds; `python -c "import fastapi, pydantic, httpx, anthropic, openai, psycopg, pgvector, deepeval, respx"` exits 0.
- [x] 1.2 Create the package tree per design D7: `app/{__init__,main}.py`, `app/contracts/`, `app/contracts/schema/`, `app/finx/`, `app/llm/`, `app/config/`, `app/store/`, `app/store/migrations/`, `tests/contracts/`, `tests/finx/`, `tests/fixtures/finx/`. Done: `python -c "import app, app.contracts, app.finx, app.llm, app.config, app.store"` exits 0.
- [x] 1.3 Add root `.env.example` with `FRESHDESK_API_KEY`, `FRESHDESK_API_ROOT`, and the DB / OpenAI / Anthropic keys the design assumes (contracts-foundation owns root files so no downstream change edits them). Done: `.env.example` lists all four key groups; no real secrets committed.

## 2. Wire contract (chat-wire-api)

- [x] 2.1 In `app/contracts/wire.py` define `SessionContext` (from `userId`, `sessionId`, `accessToken`, `isDarkTheme`, `platform`, `page`, derived `entry_surface`); `session_id`/`access_token` excluded from any response serialization. Done: `pytest tests/contracts/test_wire.py::test_session_context_hides_secrets` green.
- [x] 2.2 Define the chat request/response models (`ChatRequest`, `ChatResponse` with ordered `blocks`, `intent`, `conversation_state`, `caps`). Done: `pytest tests/contracts/test_wire.py::test_chat_envelope` green.
- [x] 2.3 Define the discriminated render-block union (11 types: `bubble`, `user_bubble`, `chip_row`, `stepper_card`, `calendar`, `file_card`, `note_list_card`, `data_card`, `error_bubble`, `ticket_confirmation`, `generating`) with the chip-action set, the CML filename carve-out on `file_card`, the note-list rows carrying an opaque session-scoped `downloadToken` (NOT `file_id`), and the data-card rows as `{label, value}` (value verbatim). Done: `pytest tests/contracts/test_wire.py::test_render_blocks` green (round-trips each block type; asserts no URL/file_id/cmlLink/email fields on file/note/data cards; note-list carries `downloadToken`).
- [x] 2.4 Define the session-seed `config_slice` embedded in the first `/api/chat` response (entry chips, greeting, client-facing limits, `whats_new`); assert server-only config (RAG tunables, calendar-bound internals, Freshdesk mapping) is excluded. Done: `pytest tests/contracts/test_wire.py::test_config_slice` green.
- [x] 2.5 Generate `app/contracts/schema/chat_wire.schema.json` from the wire union via `model_json_schema()`, check it in, and add a drift test that regenerates and diffs. Done: `pytest tests/contracts/test_wire_schema_drift.py` green (fails if the committed schema is stale).

## 3. Router contract

- [x] 3.1 In `app/contracts/router.py` define the frozen 16-value `Intent` enum (11 report + `rag_qa`, `raise_ticket`, `ticket_status`, `call_support`, `smalltalk_fallback`), `Segment` (`equity`/`fno`/`commodity`), `ReportFormat` (`pdf`/`excel`), `ExtractedParams`, `ConversationContext` (bootstrap fields + history refs + `turn_number` + `follow_up_count` + sticky-language state), `RouterResult` (`intent`, `extracted_params`, `needs_confirmation`, `follow_up_question`, `detected_language`, `escalate`, `education_line`), and the raise-ticket tool request/result types (field mapping is ticketing-owned config, not remote-config). Done: `pytest tests/contracts/test_router.py::test_intent_enum_complete` (asserts exactly 16 values incl. blocked `report_holding`/`report_global_detail`) and `::test_router_result_fields` green.
- [x] 3.2 Encode intent-precedence and follow-up-cap constants (tax > p&l; cg → tax; holding-statement → holding). Done: `pytest tests/contracts/test_router.py::test_precedence_constants` green.
- [x] 3.3 In `app/contracts/rag.py` define the frozen RAG types `RetrievedChunk` (chunk id, text, source/entry id, vector score, fts rank, fused score), `RagAnswer` (answer text, citations→chunk ids, refusal flag + refusal-reason enum, `retrieval_context`), and the canonical `retrieval_context: list[str]` alias. Done: `pytest tests/contracts/test_rag.py` green.
- [x] 3.4 In `app/contracts/tools.py` define the 10 frozen native tool definitions (`name`, `description`, `input_schema`, `strict: true`) — `route` (input_schema from `RouterResult`), `get_pnl_report`, `get_ledger_report` (mtf flag), `get_contract_notes`, `get_tax_report`, `get_cml`, `get_brokerage_slabs`, `search_kb`, `raise_ticket`, `get_ticket_status`; input_schemas generated from the frozen Pydantic models (strict-tool-use subset: `additionalProperties:false`, `required` set, unsupported constraints stripped). Generate `app/contracts/schema/tools.schema.json` and add a drift test. Done: `pytest tests/contracts/test_tools.py::test_tool_names_and_strict` (asserts the 10 frozen names, `strict:true`) and `tests/contracts/test_tools_schema_drift.py` green.

## 4. FinX envelopes, models, interfaces (finx-client)

- [x] 4.1 In `app/finx/envelopes.py` implement `parse_dotnet_envelope`, `parse_go_envelope`, `parse_mis_envelope` returning `ParsedEnvelope{outcome, payload, reason}`; no-data is reason-set based (both "Data not found." and "Data not available."); auth failure keyed off an HTTP-401 flag. Done: `pytest tests/finx/test_envelopes.py` green.
- [x] 4.2 In `app/finx/interfaces.py` define the `FinXClient` facade Protocol + 5 adapter Protocols (`DotNetMiddlewareAdapter`, `GoMiddlewareAdapter`, `MisReportsAdapter`, `MfProfileAdapter`, `FinxOmneCotiAdapter`) with their auth/casing documented per design D2. Done: `pytest tests/finx/test_interfaces.py::test_five_adapters_defined` green.
- [x] 4.3 In `app/finx/models.py` define typed request/response models + `EndpointSpec` descriptors pinning per-endpoint identity fields and enum semantics (LoginId JIFFY vs client-code; UserId neuron; GROUP1 vs Group1; Margin 0/1[CONFIRM]; RequestFor 0/2/1 per endpoint; With_Exp int vs bool; contract-note rows keyed by file_id). Mark `[CONFIRM]`/`[GAP]` verbatim. Done: `pytest tests/finx/test_models.py` green.
- [x] 4.4 Copy the sanitized 2026-07-16 capture JSON from `docs/finx_api_captures_2026-07-16.md` and the response bodies in `03_finx_api_reference.md` into `tests/fixtures/finx/*.json` (PNL download/email/no-data, Ledger PDF success/no-data, CML success/401, contract-note list success/204, Tax failure, brokerage hybrid, .NET 401, MIS 401, GetGlobalPNLNew object + falsy-With_Exp array, GetDetailedPNL array, GetLedgerDetails array). Done: fixtures contain only placeholders (no real SessionId/JWT/ClientId/email); `pytest tests/finx/test_envelopes.py tests/finx/test_models.py` exercises every parser outcome against a fixture.

## 5. Flow-engine contract

- [x] 5.1 In `app/contracts/flow.py` define `FlowState`, `Step` (with `kind`), transition types, `FlowConfig` (per-flow date windows read from config), and **implement** the FY helpers as pure date math (`currentFY`, `supportedFYs`, `defaultFY`, short↔long mapping) — shared by engine/tax-flow/router, not just typed. Done: `pytest tests/contracts/test_flow.py::test_fy_helpers` (executes the helpers across an Apr-1 boundary; asserts dynamic FY, no hardcoded years) green.
- [x] 5.2 Define byte-validation, one-silent-retry, and 15-min per-selection cache semantics as typed config, plus the `FlowSpec` protocol and the module-level `FLOW: FlowSpec` registration contract (no `register_flow` decorator, no registration imports — importlib discovery reads each module's `FLOW`, per plan Hard Rule 5). Done: `pytest tests/contracts/test_flow.py::test_cache_and_registry_contract` green.

## 6. Config (remote-config)

- [x] 6.1 In `app/config/schema.py` define `RemoteConfig` (limits, per-surface chips, greeting pool, whats_new ≤3, product list, compliance footer, per-flow calendar bounds, and RAG tunables `rag_candidate_k`/`rrf_k`/`rag_context_k`/`reranker`). No Freshdesk schema here — the ticket field mapping is ticketing-owned (`app/ticketing/freshdesk.yaml`). Done: `pytest tests/contracts/test_config.py::test_schema` green.
- [x] 6.2 In `app/config/defaults.py` provide Phase-1 defaults (page size 10, note threshold 50, message cap 10, follow-up cap 2; per-flow date windows; greeting variants; RAG tunables 25/60/5/"none"). Done: `pytest tests/contracts/test_config.py::test_defaults_validate` green (defaults validate against the schema).
- [x] 6.3 In `app/config/db.py` define the shared async engine + session factory (the single access point for the `qa_chunks` read path and the conversation-store write path); register the pgvector adapter. Done: `pytest tests/contracts/test_db.py::test_engine_factory` green (constructs the factory without opening a live connection).

## 7. Tracing, error taxonomy, LLM client

- [x] 7.1 In `app/contracts/tracing.py` define `SpanType` (agent/retriever/llm/tool), the `mask` function signature, and the `trace_manager.configure(...)` setup contract. Done: `pytest tests/contracts/test_tracing.py` green.
- [x] 7.2 In `app/contracts/errors.py` define the `ErrorCode` enum (exactly 5) and the `ErrorCopy` config with §8.4 verbatim copy + recovery chips + EC-12. Done: `pytest tests/contracts/test_errors.py::test_five_codes_verbatim` green.
- [x] 7.3 In `app/llm/client.py` define the pinned-model wrapper contract (default `claude-sonnet-5`, toggle `claude-haiku-4-5-20251001`, model from config; no sampling params exposed; `thinking` omitted [adaptive default]; `max_tokens` ~16000 non-streaming default; returns text + usage + `stop_reason`; llm span) and pass `tools=`/`tool_choice=`/`output_config.format` through unchanged (structured non-tool output via `output_config.format` json_schema — never the deprecated top-level `output_format`; no free-text JSON parsing anywhere). Done: `pytest tests/contracts/test_llm_client.py::test_pinned_models` and `::test_tool_passthrough` green (no network call; asserts model IDs, absence of temperature/top_p/top_k, and that tools/tool_choice/output_config pass through).

## 8. Conversation-store migration

- [x] 8.1 Write `app/store/migrations/0001_conversation_store.sql` creating `threads` and `turns` (columns per the conversation-store spec) + `schema_migrations`, with indexes on `turns(thread_id)` and `turns(thread_id, turn_number)` and the FK. Add `app/store/schema.py` typed row models, `app/store/migrations/runner.py`, and `app/contracts/store.py` with the frozen `TurnRecord` DTO mirroring the `0001` columns (producer=orchestrator `enqueue`, consumer=store-writer `insert`; `retrieval_context` uses the canonical `list[str]` from `app/contracts/rag.py`). Done: runner applies `0001` against a scratch Postgres (or dry-run parse) and is idempotent on re-run; `pytest tests/contracts/test_store_schema.py` green (asserts `TurnRecord` fields correspond 1:1 with the `0001` columns).

## 9. Chat endpoint stub

- [x] 9.1 In `app/main.py` create the FastAPI app with a `POST /api/chat` stub whose first (session-seed) response returns a valid `ChatResponse` — greeting bubble + starter chips + the `config_slice` — using the wire contract, making no live FinX/DB calls. Record in the module docstring that `app/main.py` is stub-here / conversation-orchestrator-owned thereafter (no other change edits it; store-writer/tracing contribute via startup hooks main.py calls). Done: `pytest tests/contracts/test_main_stub.py` green (TestClient POST returns a schema-valid response with ordered blocks and a `config_slice`).

## 10. CI target

- [x] 10.1 Wire the contract/finx test suites as the CI gate: `pytest tests/contracts tests/finx`. Done: `pytest tests/contracts tests/finx` runs green locally with no network and no live DB (fixture-based only).
