"""Choice Jini backend — Phase 1.

This package holds the frozen contracts landed by the Wave 0 ``contracts-foundation``
change: wire types, router I/O, FinX adapter interfaces + envelope parsers, flow
state-machine types, remote-config schema, tracing conventions, error taxonomy, the
LLM client wrapper, and the conversation-store migration. Downstream changes import
these contracts and are forbidden from editing the frozen surface.
"""
