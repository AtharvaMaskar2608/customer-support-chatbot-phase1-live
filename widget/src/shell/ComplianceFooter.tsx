/** Persistent compliance footer — always visible under the composer. */
export function ComplianceFooter() {
  return (
    <>
      <div className="disc">Factual answers only — never investment advice.</div>
      <div className="trust">Files land right here — no email verification.</div>
    </>
  );
}
