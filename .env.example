# Copy to .env and fill in real values. .env is gitignored — never commit it.

# ---- LLM / embeddings / evaluation ----
ANTHROPIC_API_KEY=          # Claude: router + RAG generation
OPENAI_API_KEY=             # embeddings for runtime query vectors (text-embedding-3-large, 3072d)
CONFIDENT_API_KEY=          # DeepEval trace export (optional; tracing works locally without it)

# ---- Database ----
# local dev goes through the SSH tunnel on :5433; prod uses host=10.132.148.130 port=5432
DATABASE_URL=postgresql+psycopg://atharva:__PASSWORD__@localhost:5433/customer-support-chatbot

# ---- Choice / FinX vendor auth (payload encryption + vendor keys) ----
CHOICE_VENDOR_ID=
CHOICE_VENDOR_KEY=
CHOICE_ENCRYPTION_KEY=
CHOICE_ENCRYPTED_IV=
CHOICE_FINX_API_KEY=
CHOICE_MOBILE_NO=           # care number? test mobile? — confirm usage
CHOICE_SIGNAL_INSTRUMENT=   # purpose TBD

# ---- Notifications (purpose in this project TBD) ----
SLACK_APP_ID=
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_SIGNING_SECRET=
SLACK_VERIFICATION_TOKEN=
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_ENABLED=false
